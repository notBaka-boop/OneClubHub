<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home - OneClubHub</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body {
            background: linear-gradient(135deg, #1a1a1a 0%, #2d2d2d 100%);
            min-height: 100vh;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: #ffffff;
        }

        .sidebar {
            background: rgba(33, 33, 33, 0.95);
            border-right: 1px solid rgba(255, 255, 255, 0.1);
            height: 100vh;
            position: fixed;
            width: 250px;
            padding: 20px;
            backdrop-filter: blur(10px);
        }

        .sidebar .logo {
            text-align: center;
            padding: 20px 0;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            margin-bottom: 20px;
        }

        .sidebar .logo h2 {
            background: linear-gradient(45deg, #00ff88, #00b3ff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            font-weight: 600;
            margin: 0;
        }

        .nav-link {
            color: #b3b3b3;
            padding: 12px 20px;
            border-radius: 8px;
            margin: 5px 0;
            transition: all 0.3s ease;
        }

        .nav-link:hover {
            color: #ffffff;
            background: rgba(255, 255, 255, 0.1);
            transform: translateX(5px);
        }

        .nav-link i {
            margin-right: 10px;
            width: 20px;
            text-align: center;
        }

        .main-content {
            margin-left: 250px;
            padding: 30px;
        }

        .welcome-section {
            background: rgba(45, 45, 45, 0.5);
            border-radius: 15px;
            padding: 30px;
            margin-bottom: 30px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
        }

        .welcome-title {
            font-size: 2rem;
            font-weight: 600;
            margin-bottom: 15px;
            background: linear-gradient(45deg, #00ff88, #00b3ff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .welcome-text {
            color: #b3b3b3;
            font-size: 1.1rem;
        }

        .stats-card {
            background: rgba(45, 45, 45, 0.5);
            border-radius: 15px;
            padding: 25px;
            margin-bottom: 20px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            transition: all 0.3s ease;
            backdrop-filter: blur(10px);
        }

        .stats-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
            border-color: #00ff88;
        }

        .stats-icon {
            font-size: 2rem;
            color: #00ff88;
            margin-bottom: 15px;
        }

        .stats-number {
            font-size: 1.8rem;
            font-weight: 600;
            color: #ffffff;
            margin-bottom: 5px;
        }

        .stats-label {
            color: #b3b3b3;
            font-size: 1rem;
        }

        .recent-activity {
            background: rgba(45, 45, 45, 0.5);
            border-radius: 15px;
            padding: 25px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
        }

        .activity-title {
            font-size: 1.5rem;
            font-weight: 600;
            margin-bottom: 20px;
            background: linear-gradient(45deg, #00ff88, #00b3ff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .activity-item {
            padding: 15px 0;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        .activity-item:last-child {
            border-bottom: none;
        }

        .activity-text {
            color: #b3b3b3;
            margin: 0;
        }

        .activity-time {
            color: #808080;
            font-size: 0.9rem;
        }

        @media (max-width: 768px) {
            .sidebar {
                width: 100%;
                height: auto;
                position: relative;
            }
            .main-content {
                margin-left: 0;
            }
        }
    </style>
</head>
<body>
    <!-- Sidebar -->
    <div class="sidebar">
        <div class="logo">
            <h2>OneClubHub</h2>
        </div>
        <nav class="nav flex-column">
            <a class="nav-link active" href="/home">
                <i class="fas fa-home"></i> Home
            </a>
            <a class="nav-link" href="/myprofile">
                <i class="fas fa-user"></i> Profile
            </a>
            <a class="nav-link" href="/notification">
                <i class="fas fa-bell"></i> Notifications
            </a>
            <a class="nav-link" href="/myclub">
                <i class="fas fa-users"></i> My Clubs
            </a>
            <a class="nav-link" href="/chat">
                <i class="fas fa-comments"></i> Chat
            </a>
            <a class="nav-link" href="/logout">
                <i class="fas fa-sign-out-alt"></i> Logout
            </a>
        </nav>
    </div>

    <!-- Main Content -->
    <div class="main-content" style="margin-top: 80px;">
        <!-- Welcome Section -->
        <section class="section">
            <div class="container">
                <div class="welcome-section">
                    <h1 class="welcome-title">Welcome back, <%= user.name %>!</h1>
                    <p class="welcome-text">Here's what's happening with your clubs today.</p>
                </div>
            </div>
        </section>

        <!-- Stats Section -->
        <div class="row">
            <div class="col-md-4">
                <div class="stats-card">
                    <i class="fas fa-users stats-icon"></i>
                    <div class="stats-number">5</div>
                    <div class="stats-label">Active Clubs</div>
                </div>
            </div>
            <div class="col-md-4">
                <div class="stats-card">
                    <i class="fas fa-calendar-check stats-icon"></i>
                    <div class="stats-number">3</div>
                    <div class="stats-label">Upcoming Events</div>
                </div>
            </div>
            <div class="col-md-4">
                <div class="stats-card">
                    <i class="fas fa-bell stats-icon"></i>
                    <div class="stats-number">2</div>
                    <div class="stats-label">New Notifications</div>
                </div>
            </div>
        </div>

        <!-- Recent Activity -->
        <div class="recent-activity mt-4">
            <h2 class="activity-title">Recent Activity</h2>
            <div class="activity-item">
                <p class="activity-text">New event announced: Web Development Workshop</p>
                <small class="activity-time">2 hours ago</small>
            </div>
            <div class="activity-item">
                <p class="activity-text">New member joined PICT CSI</p>
                <small class="activity-time">5 hours ago</small>
            </div>
            <div class="activity-item">
                <p class="activity-text">Meeting scheduled for tomorrow</p>
                <small class="activity-time">1 day ago</small>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 