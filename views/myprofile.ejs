<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Profile - OneClubHub</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body {
            background: linear-gradient(135deg, #1a1a1a 0%, #2d2d2d 100%);
            min-height: 100vh;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: #ffffff;
        }

        .sidebar {
            background: rgba(33, 33, 33, 0.95);
            border-right: 1px solid #404040;
            height: 100vh;
            position: fixed;
            width: 250px;
            padding: 20px;
        }

        .sidebar .logo {
            text-align: center;
            padding: 20px 0;
            border-bottom: 1px solid #404040;
            margin-bottom: 20px;
        }

        .sidebar .logo h2 {
            color: #ffffff;
            font-weight: 600;
            margin: 0;
        }

        .nav-link {
            color: #b3b3b3;
            padding: 12px 20px;
            border-radius: 8px;
            margin: 5px 0;
            transition: all 0.3s ease;
        }

        .nav-link:hover {
            color: #ffffff;
            background: rgba(255, 255, 255, 0.1);
        }

        .nav-link i {
            margin-right: 10px;
            width: 20px;
            text-align: center;
        }

        .main-content {
            margin-left: 250px;
            padding: 30px;
        }

        .profile-header {
            background: rgba(45, 45, 45, 0.5);
            border-radius: 15px;
            padding: 30px;
            margin-bottom: 30px;
            border: 1px solid #404040;
            display: flex;
            align-items: center;
            gap: 30px;
        }

        .profile-avatar {
            width: 120px;
            height: 120px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 3rem;
            color: #808080;
            overflow: hidden;
        }

        .profile-avatar img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .profile-info h1 {
            font-size: 2rem;
            font-weight: 600;
            margin: 0 0 10px;
            color: #ffffff;
        }

        .profile-info p {
            color: #b3b3b3;
            margin: 0;
            font-size: 1.1rem;
        }

        .profile-stats {
            display: flex;
            gap: 20px;
            margin-top: 15px;
        }

        .stat-item {
            text-align: center;
            background: rgba(255, 255, 255, 0.05);
            padding: 10px 20px;
            border-radius: 10px;
            border: 1px solid #404040;
            min-width: 120px;
        }

        .stat-number {
            font-size: 1.5rem;
            font-weight: 600;
            color: #ffffff;
            margin-bottom: 5px;
        }

        .stat-label {
            color: #b3b3b3;
            font-size: 0.9rem;
        }

        .stat-item i {
            color: #808080;
            margin-right: 5px;
        }

        .profile-section {
            background: rgba(45, 45, 45, 0.5);
            border-radius: 15px;
            padding: 25px;
            margin-bottom: 20px;
            border: 1px solid #404040;
        }

        .section-title {
            font-size: 1.3rem;
            font-weight: 600;
            margin-bottom: 20px;
            color: #ffffff;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .section-title i {
            color: #808080;
        }

        .info-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 20px;
        }

        .info-item {
            background: rgba(255, 255, 255, 0.05);
            border-radius: 10px;
            padding: 15px;
            border: 1px solid #404040;
        }

        .info-label {
            color: #808080;
            font-size: 0.9rem;
            margin-bottom: 5px;
        }

        .info-value {
            color: #ffffff;
            font-size: 1.1rem;
        }

        .club-list {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 20px;
        }

        .club-item {
            background: rgba(255, 255, 255, 0.05);
            border-radius: 10px;
            padding: 15px;
            border: 1px solid #404040;
            transition: all 0.3s ease;
        }

        .club-item:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
            border-color: #666666;
        }

        .club-name {
            font-size: 1.1rem;
            font-weight: 600;
            color: #ffffff;
            margin-bottom: 5px;
        }

        .club-role {
            color: #b3b3b3;
            font-size: 0.9rem;
        }

        .btn {
            padding: 8px 20px;
            border-radius: 8px;
            font-weight: 500;
            transition: all 0.3s ease;
        }

        .btn-primary {
            background: #404040;
            border: none;
            color: #ffffff;
        }

        .btn-primary:hover {
            background: #4d4d4d;
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
        }

        .btn-outline {
            background: transparent;
            border: 1px solid #404040;
            color: #b3b3b3;
        }

        .btn-outline:hover {
            border-color: #666666;
            color: #ffffff;
            transform: translateY(-2px);
        }

        /* Edit Profile Form Styles */
        .edit-form {
            display: none;
            background: rgba(45, 45, 45, 0.95);
            border-radius: 15px;
            padding: 25px;
            margin-top: 20px;
            border: 1px solid #404040;
        }

        .edit-form.active {
            display: block;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            color: #b3b3b3;
            margin-bottom: 8px;
            display: block;
        }

        .form-control {
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid #404040;
            border-radius: 8px;
            color: #ffffff;
            padding: 10px 15px;
            width: 100%;
        }

        .form-control:focus {
            border-color: #666666;
            outline: none;
            box-shadow: 0 0 0 2px rgba(255, 255, 255, 0.1);
        }

        .form-actions {
            display: flex;
            gap: 10px;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <!-- Sidebar -->
    <div class="sidebar">
        <div class="logo">
            <h2>OneClubHub</h2>
        </div>
        <nav class="nav flex-column">
            <a class="nav-link" href="/home">
                <i class="fas fa-home"></i> Home
            </a>
            <a class="nav-link active" href="/myprofile">
                <i class="fas fa-user"></i> Profile
            </a>
            <a class="nav-link" href="/notification">
                <i class="fas fa-bell"></i> Notifications
            </a>
            <a class="nav-link" href="/myclub">
                <i class="fas fa-users"></i> My Clubs
            </a>
            <a class="nav-link" href="/logout">
                <i class="fas fa-sign-out-alt"></i> Logout
            </a>
        </nav>
    </div>

    <!-- Main Content -->
    <div class="main-content">
        <!-- Profile Header -->
        <div class="profile-header">
            <div class="profile-avatar">
                <img src="images/ameya.jpg" alt="Ameya Gokhale">
            </div>
            <div class="profile-info">
                <h1>Ameya Gokhale</h1>
                <p><%= user.email %></p>
                <div class="profile-stats">
                    <div class="stat-item">
                        <i class="fas fa-users"></i>
                        <div class="stat-number">3</div>
                        <div class="stat-label">Clubs</div>
                    </div>
                    <div class="stat-item">
                        <i class="fas fa-calendar-check"></i>
                        <div class="stat-number">12</div>
                        <div class="stat-label">Events</div>
                    </div>
                    <div class="stat-item">
                        <i class="fas fa-clock"></i>
                        <div class="stat-number">45</div>
                        <div class="stat-label">Hours Contributed</div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Personal Information -->
        <div class="profile-section">
            <h2 class="section-title">
                <i class="fas fa-info-circle"></i>
                Personal Information
            </h2>
            <div class="info-grid">
                <div class="info-item">
                    <div class="info-label">Enrollment Number</div>
                    <div class="info-value"><%= user.enrollment %></div>
                </div>
                <div class="info-item">
                    <div class="info-label">Branch</div>
                    <div class="info-value"><%= user.branch %></div>
                </div>
                <div class="info-item">
                    <div class="info-label">Year</div>
                    <div class="info-value">3rd Year</div>
                </div>
                <div class="info-item">
                    <div class="info-label">Status</div>
                    <div class="info-value">Active</div>
                </div>
            </div>
        </div>

        <!-- My Clubs -->
        <div class="profile-section">
            <h2 class="section-title">
                <i class="fas fa-users"></i>
                My Clubs
            </h2>
            <div class="club-list">
                <div class="club-item">
                    <div class="club-name">PICT CSI</div>
                    <div class="club-role">Member</div>
                </div>
                <div class="club-item">
                    <div class="club-name">PICT ACM</div>
                    <div class="club-role">Core Member</div>
                </div>
                <div class="club-item">
                    <div class="club-name">PICT PISB</div>
                    <div class="club-role">Member</div>
                </div>
            </div>
        </div>

        <!-- Actions -->
        <div class="profile-section">
            <h2 class="section-title">
                <i class="fas fa-cog"></i>
                Account Actions
            </h2>
            <div class="d-flex gap-3">
                <button class="btn btn-primary" onclick="toggleEditForm()">Edit Profile</button>
                <button class="btn btn-outline">Change Password</button>
            </div>

            <!-- Edit Profile Form -->
            <div class="edit-form" id="editProfileForm">
                <h3 class="section-title">Edit Profile Information</h3>
                <form id="profileEditForm" onsubmit="saveProfileChanges(event)">
                    <div class="form-group">
                        <label for="name">Full Name</label>
                        <input type="text" class="form-control" id="name" value="Ameya Gokhale">
                    </div>
                    <div class="form-group">
                        <label for="email">Email</label>
                        <input type="email" class="form-control" id="email" value="<%= user.email %>" readonly>
                    </div>
                    <div class="form-group">
                        <label for="enrollment">Enrollment Number</label>
                        <input type="text" class="form-control" id="enrollment" value="<%= user.enrollment %>">
                    </div>
                    <div class="form-group">
                        <label for="branch">Branch</label>
                        <input type="text" class="form-control" id="branch" value="<%= user.branch %>">
                    </div>
                    <div class="form-group">
                        <label for="year">Year</label>
                        <select class="form-control" id="year">
                            <option value="1st Year">1st Year</option>
                            <option value="2nd Year">2nd Year</option>
                            <option value="3rd Year" selected>3rd Year</option>
                            <option value="4th Year">4th Year</option>
                        </select>
                    </div>
                    <div class="form-actions">
                        <button type="submit" class="btn btn-primary">Save Changes</button>
                        <button type="button" class="btn btn-outline" onclick="toggleEditForm()">Cancel</button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        function toggleEditForm() {
            const form = document.getElementById('editProfileForm');
            form.classList.toggle('active');
        }

        function saveProfileChanges(event) {
            event.preventDefault();
            
            // Get form values
            const name = document.getElementById('name').value;
            const enrollment = document.getElementById('enrollment').value;
            const branch = document.getElementById('branch').value;
            const year = document.getElementById('year').value;

            // Update profile display
            document.querySelector('.profile-info h1').textContent = name;
            document.querySelector('.info-item:nth-child(1) .info-value').textContent = enrollment;
            document.querySelector('.info-item:nth-child(2) .info-value').textContent = branch;
            document.querySelector('.info-item:nth-child(3) .info-value').textContent = year;

            // Hide the form
            toggleEditForm();

            // Show success message
            alert('Profile updated successfully!');
        }
    </script>
</body>
</html>
